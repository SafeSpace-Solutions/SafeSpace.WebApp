<!-- Follow these steps for the initial setup 

1. Download and install nvm-windows from https://github.com/coreybutler/nvm-windows/releases

2. Installing Node.js 16.14.2:

   Open CMD and type the following commands:
	nvm install 16.14.2
	nvm use 16.14.2
   [Installing this Node version will also install the npm version that's bundled with it.]

3. Installing npm 8.5.0:

   In the CMD, type the following command:
	 npm install -g npm@8.5.0

4. Installing Angular CLI 16.2.0:

   In the CMD, type the following command:
	npm install -g @angular/cli@16.2.0

5. Verification

   After running these commmands in the Windows CMD, use the following commands to verify the versions:

	node -v
	npm -v
	ng version -->